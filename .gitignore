# Vercel
.vercel

# Node (only used for the local static server / vercel CLI via npx)
node_modules/
npm-debug.log*
yarn-error.log*

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Local env (not used by this project, but just in case)
.env
.env.local
